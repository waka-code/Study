# JSX

JSX es una sintaxis que combina JavaScript y XML, permitiendo escribir componentes de UI de forma más legible.

- Se transforma en llamadas a `React.createElement()`.
- Permite incrustar expresiones JS entre `{}`.

```jsx
const element = <h1>Hello, {user.name}!</h1>;
```
