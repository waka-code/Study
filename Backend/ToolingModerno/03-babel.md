# Babel

Babel es un transpilador de JavaScript que convierte código moderno (ES6+) a versiones compatibles con navegadores antiguos.

**Ventajas:**
- Permite usar sintaxis moderna.
- Plugins para transformar JSX, TypeScript, etc.
- Integración con Webpack y otros bundlers.

**Uso básico:**
- Configuración en `.babelrc` o `babel.config.js`.
- Ejecuta `npx babel src --out-dir dist` para transpilar.
