# Herencia

La herencia permite crear nuevas clases basadas en clases existentes, reutilizando y extendiendo su funcionalidad.

**Ejemplo:**
Una clase `Perro` hereda de `Animal` y añade su propio comportamiento.
