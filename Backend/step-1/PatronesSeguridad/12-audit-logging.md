# Audit Logging

Registra eventos importantes para detectar y analizar incidentes de seguridad.

**Ejemplo:**
- Log de accesos, cambios, errores
- Almacena logs de forma segura

```js
console.log(`[AUDIT] Usuario ${userId} accedió a recurso`);
```
