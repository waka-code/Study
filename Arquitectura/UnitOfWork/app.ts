// Ejemplo básico de Unit of Work en TypeScript
// import { UnitOfWork } from './UnitOfWork';

// const uow = new UnitOfWork();
// uow.addOperation(() => console.log('Operación 1'));
// uow.addOperation(() => console.log('Operación 2'));
// uow.commit();
