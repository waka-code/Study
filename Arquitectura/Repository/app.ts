// Ejemplo básico de Repository en TypeScript
// import { InMemoryUserRepository } from './Repository';

// const repo = new InMemoryUserRepository();
// repo.save({ id: 1, name: 'Ana' });
// console.log(repo.findById(1));
