// Ejemplo básico de CQRS en TypeScript
// import { UserHandler, CreateUserCommand, GetUserQuery } from './CQRS';

// const handler = new UserHandler();
// handler.handleCommand(new CreateUserCommand('Luis'));
// console.log(handler.handleQuery(new GetUserQuery(1)));
